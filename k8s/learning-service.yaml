apiVersion: apps/v1
kind: Deployment
metadata:
  name: learning-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: learning-service
  template:
    metadata:
      labels:
        app: learning-service
    spec:
      containers:
        - name: learning-service
          image: educationalapp-learning-service:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 3002
---
apiVersion: v1
kind: Service
metadata:
  name: learning-service
spec:
  selector:
    app: learning-service
  ports:
    - port: 3002
      targetPort: 3002
